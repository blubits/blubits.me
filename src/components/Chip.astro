---
interface Props {
  bg_color?: string;
  classNames?: string;
  isCode?: boolean;
}

const {
  bg_color = "bg-white bg-opacity-10",
  classNames = "",
  isCode = false,
} = Astro.props;
---

{
  () => {
    if (isCode) {
      return (
        <code
          class:list={[
            "inline-flex items-center gap-x-2 font-mono self-start",
            "rounded-sm px-2 py-0.5",
            bg_color,
            classNames,
          ]}
        >
          <slot />
        </code>
      );
    } else {
      return (
        <span
          class:list={[
            "inline-flex items-center gap-x-2 self-start",
            "rounded-sm px-2 py-0.5",
            bg_color,
            classNames,
          ]}
        >
          <slot />
        </span>
      );
    }
  }
}
